---
layout: base.njk
permalink: /blog/index.html
eleventyExcludeFromCollections: true
title: 開公司到找空間，中小企業會遇到的事
metaTitle: 工商登記、公司設立、辦公空間指南｜貝森國際商務中心
lineSrc: seo_index
description: 從開公司、辦工商登記，到找辦公室、租會議室——貝森國際商務中心把中小企業一路上最常卡住的問題，用服務過幾百家企業的經驗整理成可以照著做的指南。
---

<div class="art">

  <nav class="crumb" aria-label="麵包屑">
    <a href="/">首頁</a><i>›</i><span>經營指南</span>
  </nav>

  <header class="list-head">
    <span class="eyebrow">經營指南</span>
    <h1>開公司到找空間，<br>中小企業會遇到的事</h1>
    <p>工商登記怎麼辦、地址放哪裡、辦公室怎麼挑、會議室要注意什麼——這些問題我們每天在櫃檯被問。與其一次一次回答，不如整理成你可以照著做的指南。</p>
  </header>

  <div class="chips">
    <a href="/blog/" class="chip on">全部<em>{{ collections.posts.length }}</em></a>
    {%- for c in collections.categories %}
    <a href="/blog/c/{{ c.slug }}/" class="chip">{{ c.title }}<em>{{ c.count }}</em></a>
    {%- endfor %}
  </div>

  {%- if collections.posts.length %}
  <div class="cards">
    {%- for p in collections.posts %}
    <a class="card" href="{{ p.url }}">
      <div class="k">{{ p.data.category or "商務知識" }}</div>
      <h3>{{ p.data.title }}</h3>
      <p>{{ p.data.description | truncate(74) }}</p>
      <div class="d">{{ (p.data.updated or p.date) | twDate }}</div>
    </a>
    {%- endfor %}
  </div>
  {%- else %}
  <p class="empty">文章準備中，敬請期待。</p>
  {%- endif %}

</div>
